using UnityEngine;

[CreateAssetMenu(menuName = "Create Person", fileName = "New Person")]
public class Person : ScriptableObject
{
    public Sprite spritePerson;
    public string name;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
